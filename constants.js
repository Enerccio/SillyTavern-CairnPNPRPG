import {EXTENSION_NAME, EXTENSION_PATH, MODULE_NAME, VERSION} from './conf.js';

export const DICE_ROLL_FUNCTION = `${MODULE_NAME}_request_dice_roll`;
export const ATTACK_FUNCTION = `${MODULE_NAME}_attack`;

export const INTERNAL_STATE = "internal_state";
export const INTERNAL_STATE_XML = `${MODULE_NAME}:${INTERNAL_STATE}`;
